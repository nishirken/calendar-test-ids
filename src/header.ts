import { makeTestIdSelectors } from "test-ids";

export const headerTestIds = {
  root: "header",
  todayButton: "today-button",
  arrowLeft: "arrow-left",
  arrowRight: "arrow-right",
  date: "date",
};

export const headerTestIdSelectors = makeTestIdSelectors(headerTestIds);
